<section class="contact-hero position-relative" style="overflow:hidden; background:linear-gradient(150deg,#0a1628 0%,#0f2b5e 45%,#1a4fd8 100%); color:#fff;">
  <div class="position-absolute rounded-circle" style="width:650px;height:650px;background:radial-gradient(circle,rgba(59,130,246,0.2) 0%,transparent 70%);top:-220px;right:-240px;pointer-events:none;animation:orbDrift 18s ease-in-out infinite alternate;"></div>
  <div class="position-absolute rounded-circle" style="width:520px;height:520px;background:radial-gradient(circle,rgba(96,165,250,0.12) 0%,transparent 70%);bottom:-160px;left:-200px;pointer-events:none;animation:orbDrift 20s ease-in-out infinite alternate-reverse;"></div>
  <div class="position-absolute top-0 start-0 w-100 h-100" style="background-image:radial-gradient(rgba(255,255,255,0.04) 1px,transparent 1px);background-size:40px 40px;opacity:.35;pointer-events:none;"></div>
  <div class="container position-relative py-5" style="z-index:2;">
    <div class="text-center" data-aos="fade-up">
      <span class="contact-chip">Reach Out</span>
      <h1 class="display-5 fw-bold mt-3 mb-2" style="letter-spacing:-1.3px;">Contact <span style="color:#60a5fa;">CALIDRO RACS</span></h1>
      <p class="fs-5 text-white-50 mx-auto" style="max-width:620px;">
        We're here to help—reach out for bookings, support, or feedback to keep your home running comfortably.
      </p>
    </div>
  </div>
</section>

<section class="support-premium position-relative py-5" style="overflow:hidden; background:linear-gradient(180deg,#eff6ff 0%,#e0edff 60%,#ffffff 100%);">
  <div class="position-absolute top-0 start-0 w-100 h-100" style="background-image:radial-gradient(rgba(59,130,246,0.08) 1px,transparent 1px);background-size:60px 60px;opacity:0.35;pointer-events:none;"></div>
  <div class="container position-relative" style="z-index:1;">
    <div class="text-center mb-4" data-aos="fade-up">
      <span class="contact-chip light">Support Channels</span>
      <h2 class="fw-bold mt-3 mb-2" style="color:#0a1628;">Choose the help you need</h2>
      <p class="text-muted mb-0" style="max-width:640px;margin:0 auto;">Call or message us for bookings, or send detailed feedback to help us level up every visit.</p>
    </div>

    <div class="support-toggle-group d-flex justify-content-center flex-wrap gap-2 mb-4" role="tablist">
      <button class="support-toggle active" type="button" data-target="contact-support">Support & Bookings</button>
      <button class="support-toggle" type="button" data-target="contact-feedback">Service feedback</button>
    </div>

    <div class="support-panels">
      <div class="support-panel active" id="contact-support" data-aos="fade-right">
        <div class="support-card">
          <div class="d-flex flex-wrap gap-3 justify-content-between align-items-start mb-4">
            <div>
              <p class="eyebrow mb-1">Support & Bookings</p>
              <h3 class="fw-bold mb-1" style="color:#0a1628;">Get in touch</h3>
              <p class="text-muted mb-0">Call, message, or visit during business hours.</p>
            </div>
            <div class="support-icon gradient"><i class="bi bi-headset"></i></div>
          </div>
          <div class="row g-4">
            <div class="col-md-6">
              <div class="support-info">
                <div class="d-flex align-items-center gap-3">
                  <div class="info-icon-circle">
                    <i class="bi bi-telephone-fill"></i>
                  </div>
                  <div>
                    <p class="label">Phone</p>
                    <a class="value" href="tel:+639656056495">09656056495</a>
                    <p class="muted">Quick bookings and urgent support</p>
                  </div>
                </div>
              </div>
            </div>
            <div class="col-md-6">
              <div class="support-info">
                <div class="d-flex align-items-center gap-3">
                  <div class="info-icon-circle email">
                    <i class="bi bi-envelope-fill"></i>
                  </div>
                  <div>
                    <p class="label">Email</p>
                    <a class="value" href="mailto:info@calidro.com">info@calidro.com</a>
                    <p class="muted">Send details, attachments, or floor plans</p>
                  </div>
                </div>
              </div>
            </div>
            <div class="col-md-6">
              <div class="support-info">
                <div class="d-flex align-items-center gap-3">
                  <div class="info-icon-circle hours">
                    <i class="bi bi-clock-fill"></i>
                  </div>
                  <div>
                    <p class="label">Hours</p>
                    <div class="value">Mon–Sat · 8:00 AM – 6:00 PM</div>
                    <p class="muted">Reach us within business hours</p>
                  </div>
                </div>
              </div>
            </div>
            <div class="col-md-6">
              <div class="support-info">
                <div class="d-flex align-items-center gap-3">
                  <div class="info-icon-circle address">
                    <i class="bi bi-geo-alt-fill"></i>
                  </div>
                  <div>
                    <p class="label">Address</p>
                    <div class="value">San Leonardo, Nueva Ecija</div>
                    <p class="muted">Drop by or schedule on-site service</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mt-4 pt-3 border-top">
            <span class="text-muted small">Follow us for real-time updates</span>
            <div class="d-flex gap-2">
              <a href="#" class="social-pill" aria-label="Facebook"><i class="bi bi-facebook"></i></a>
              <a href="#" class="social-pill" aria-label="Instagram"><i class="bi bi-instagram"></i></a>
              <a href="#" class="social-pill" aria-label="YouTube"><i class="bi bi-youtube"></i></a>
            </div>
          </div>
        </div>
      </div>

      <div class="support-panel" id="contact-feedback" data-aos="fade-left">
        <div class="support-card">
          <div class="d-flex flex-wrap gap-3 justify-content-between align-items-start mb-4">
            <div>
              <p class="eyebrow mb-1">Service feedback</p>
              <h3 class="fw-bold mb-1" style="color:#0a1628;">Send us your feedback</h3>
              <p class="text-muted mb-0">Share what we did great or what we can improve.</p>
            </div>
            <div class="support-icon amber"><i class="bi bi-stars"></i></div>
          </div>
          <form class="row g-4" id="feedbackForm">
            <div class="col-md-6">
              <label class="form-label">Your name</label>
              <div class="input-icon-wrapper">
                <i class="bi bi-person input-icon"></i>
                <input type="text" class="form-control premium-input-with-icon" placeholder="Full name" required maxlength="40" id="fName">
              </div>
            </div>
            <div class="col-md-6">
              <label class="form-label">Email</label>
              <div class="input-icon-wrapper">
                <i class="bi bi-envelope input-icon"></i>
                <input type="email" class="form-control premium-input-with-icon" placeholder="you@email.com" required id="fEmail">
              </div>
            </div>
            <div class="col-md-5">
              <label class="form-label">Rate your experience</label>
              <div class="premium-rating-wrapper">
                <div class="star-rating" id="starRating">
                  <button type="button" class="star-btn" data-value="1"><i class="bi bi-star"></i></button>
                  <button type="button" class="star-btn" data-value="2"><i class="bi bi-star"></i></button>
                  <button type="button" class="star-btn" data-value="3"><i class="bi bi-star"></i></button>
                  <button type="button" class="star-btn" data-value="4"><i class="bi bi-star"></i></button>
                  <button type="button" class="star-btn" data-value="5"><i class="bi bi-star"></i></button>
                </div>
                <span class="rating-label" id="ratingLabel">Select rating</span>
                <input type="hidden" id="fRating" required>
              </div>
            </div>
            <div class="col-md-7">
              <label class="form-label">Comments <span id="fCommentsCount" class="text-muted ms-1 small">(160 left)</span></label>
              <div class="input-icon-wrapper textarea-wrapper">
                <i class="bi bi-chat-left-text input-icon textarea-icon"></i>
                <textarea rows="3" class="form-control premium-input-with-icon textarea-input" placeholder="Share details so we can improve..." maxlength="160" required id="fComments"></textarea>
              </div>
            </div>
            <div class="col-12 d-flex flex-wrap gap-3 align-items-center">
              <button type="submit" class="btn btn-premium-feedback">Submit Feedback</button>
              <span class="text-muted small" id="feedbackStatus">Avg. response time: &lt; 24 hrs</span>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <style>
    .contact-chip {
      display:inline-flex;padding:6px 18px;border-radius:999px;border:1px solid rgba(255,255,255,0.35);
      font-size:0.78rem;letter-spacing:0.18em;text-transform:uppercase;background:rgba(255,255,255,0.14);
    }
    .contact-chip.light { background:#fff;border:1px solid rgba(15,36,96,0.08);color:#2563eb;box-shadow:0 6px 20px rgba(15,36,96,0.08); }
    .support-toggle {
      border:none;background:#fff;color:#0f172a;font-weight:600;padding:10px 26px;border-radius:999px;
      box-shadow:0 12px 35px rgba(15,36,96,0.1);border:1px solid rgba(15,36,96,0.05);transition:all .25s ease;
    }
    .support-toggle.active { background:linear-gradient(135deg,#3b82f6,#2563eb);color:#fff;box-shadow:0 16px 40px rgba(59,130,246,0.35);border-color:transparent; }
    .support-toggle:hover { transform:translateY(-2px); }
    .support-panel { display:none; }
    .support-panel.active { display:block; }
    .support-card { background:#fff;border-radius:24px;padding:40px;border:1px solid rgba(15,36,96,0.06);box-shadow:0 25px 60px rgba(15,36,96,0.12); }
    .support-icon { width:54px;height:54px;border-radius:18px;display:flex;align-items:center;justify-content:center;color:#fff;font-size:1.3rem; }
    .support-icon.gradient { background:linear-gradient(135deg,#3b82f6,#2563eb);box-shadow:0 12px 30px rgba(59,130,246,0.25); }
    .support-icon.amber { background:linear-gradient(135deg,#fbbf24,#f97316);box-shadow:0 12px 30px rgba(249,115,22,0.3); }
    .support-info { padding:20px;border-radius:18px;border:1px solid rgba(15,36,96,0.05);background:linear-gradient(145deg,#fff,#f6f8ff);box-shadow:inset 0 1px 0 rgba(255,255,255,0.8);transition:all 0.3s ease; }
    .support-info:hover { transform:translateY(-4px);box-shadow:0 12px 30px rgba(37,99,235,0.12),inset 0 1px 0 rgba(255,255,255,0.8); }
    .info-icon-circle { width:48px;height:48px;border-radius:14px;background:linear-gradient(135deg,#3b82f6,#2563eb);display:flex;align-items:center;justify-content:center;color:#fff;font-size:1.2rem;box-shadow:0 8px 20px rgba(37,99,235,0.3);transition:all 0.3s cubic-bezier(0.34,1.56,0.64,1);flex-shrink:0; }
    .info-icon-circle.email { background:linear-gradient(135deg,#10b981,#059669);box-shadow:0 8px 20px rgba(16,185,129,0.3); }
    .info-icon-circle.hours { background:linear-gradient(135deg,#f59e0b,#d97706);box-shadow:0 8px 20px rgba(245,158,11,0.3); }
    .info-icon-circle.address { background:linear-gradient(135deg,#ef4444,#dc2626);box-shadow:0 8px 20px rgba(239,68,68,0.3); }
    .support-info:hover .info-icon-circle { transform:scale(1.1) rotate(-5deg); }
    .support-info .label { text-transform:uppercase;letter-spacing:0.12em;font-size:0.72rem;font-weight:700;color:#94a3b8;margin-bottom:4px; }
    .support-info .value { font-weight:700;font-size:1.05rem;color:#0a1628;text-decoration:none;transition:color 0.2s; }
    .support-info .value:hover { color:#2563eb; }
    .support-info .muted { margin-bottom:0;font-size:0.85rem;color:#94a3b8; }
    .input-icon-wrapper { position:relative; }
    .input-icon { position:absolute;left:14px;top:50%;transform:translateY(-50%);color:#94a3b8;font-size:1.1rem;transition:all 0.3s ease;z-index:2; }
    .premium-input-with-icon { padding-left:44px;border-radius:14px;border:1.5px solid rgba(148,163,184,0.3);background:#fff;padding-top:0.9rem;padding-bottom:0.9rem;transition:all 0.3s ease; }
    .premium-input-with-icon:focus { border-color:#2563eb;box-shadow:0 0 0 4px rgba(37,99,235,0.1);outline:none; }
    .premium-input-with-icon:focus + .input-icon,.input-icon-wrapper:focus-within .input-icon { color:#2563eb;transform:translateY(-50%) scale(1.1); }
    .textarea-wrapper .input-icon { top:14px;transform:none; }
    .textarea-wrapper:focus-within .input-icon { transform:scale(1.1); }
    .textarea-input { padding-top:12px;min-height:100px;resize:vertical; }
    .premium-rating-wrapper { background:#fff;border-radius:14px;border:1.5px solid rgba(148,163,184,0.3);padding:10px 16px;display:flex;align-items:center;gap:12px;transition:all 0.3s ease; }
    .premium-rating-wrapper:focus-within { border-color:#2563eb;box-shadow:0 0 0 4px rgba(37,99,235,0.1); }
    .star-rating { display:flex;gap:4px; }
    .star-btn { background:none;border:none;padding:0;cursor:pointer;transition:all 0.2s cubic-bezier(0.34,1.56,0.64,1);font-size:1.4rem;color:#cbd5e1; }
    .star-btn:hover { transform:scale(1.2); }
    .star-btn.active { color:#fbbf24;transform:scale(1.1); }
    .star-btn.active i.bi-star::before { content:"\f586"; }
    .star-btn:hover ~ .star-btn { color:#cbd5e1; }
    .star-rating:hover .star-btn { color:#fbbf24; }
    .star-rating .star-btn:hover ~ .star-btn { color:#cbd5e1; }
    .rating-label { font-size:0.85rem;color:#64748b;font-weight:500; }
    .social-pill { width:42px;height:42px;border-radius:14px;border:1px solid rgba(59,130,246,0.2);display:flex;align-items:center;justify-content:center;color:#2563eb;background:#f8fbff;transition:all .25s ease; }
    .social-pill:hover { background:#2563eb;color:#fff;transform:translateY(-3px);box-shadow:0 12px 24px rgba(37,99,235,0.25); }
    .premium-input { border-radius:14px;border:1px solid rgba(148,163,184,0.4);background:#fdfdff;padding:0.85rem 1rem;transition:border-color .2s,box-shadow .2s; }
    .premium-input:focus { border-color:#2563eb;box-shadow:0 0 0 3px rgba(37,99,235,0.15); }
    .btn-premium-feedback { background:linear-gradient(135deg,#3b82f6,#2563eb);color:#fff;border:none;border-radius:999px;padding:0.8rem 2.8rem;font-weight:600;box-shadow:0 14px 30px rgba(59,130,246,0.3);transition:all .3s ease; }
    .btn-premium-feedback:hover { transform:translateY(-3px);box-shadow:0 18px 38px rgba(59,130,246,0.4); }
    @media (max-width: 767.98px) { .support-card { padding:26px; } .support-info { padding:14px; } }
  </style>

  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const toggles = document.querySelectorAll('.support-toggle');
      const panels = document.querySelectorAll('.support-panel');
      const nameField = document.getElementById('fName');
      const emailField = document.getElementById('fEmail');
      const commentField = document.getElementById('fComments');
      const form = document.getElementById('feedbackForm');
      const statusEl = document.getElementById('feedbackStatus');
      const counter = document.getElementById('fCommentsCount');
      const ratingInput = document.getElementById('fRating');
      const starBtns = document.querySelectorAll('.star-btn');
      const ratingLabel = document.getElementById('ratingLabel');
      const ratingLabels = ['Poor', 'Needs work', 'Okay', 'Good', 'Excellent'];
      
      // Star rating functionality
      starBtns.forEach((btn, index) => {
        btn.addEventListener('click', () => {
          const value = parseInt(btn.dataset.value);
          ratingInput.value = value;
          ratingLabel.textContent = ratingLabels[value - 1];
          updateStars(value);
        });
        btn.addEventListener('mouseenter', () => {
          updateStars(parseInt(btn.dataset.value), true);
        });
      });
      
      document.getElementById('starRating').addEventListener('mouseleave', () => {
        updateStars(parseInt(ratingInput.value) || 0);
      });
      
      function updateStars(value, isHover = false) {
        starBtns.forEach((btn, index) => {
          const starValue = index + 1;
          if (starValue <= value) {
            btn.classList.add('active');
            btn.querySelector('i').classList.remove('bi-star');
            btn.querySelector('i').classList.add('bi-star-fill');
          } else {
            btn.classList.remove('active');
            btn.querySelector('i').classList.remove('bi-star-fill');
            btn.querySelector('i').classList.add('bi-star');
          }
        });
      }
      
      toggles.forEach(btn => {
        btn.addEventListener('click', () => {
          const target = btn.getAttribute('data-target');
          toggles.forEach(t => t.classList.toggle('active', t === btn));
          panels.forEach(panel => panel.classList.toggle('active', panel.id === target));
        });
      });
      if (toggles.length) toggles[0].click();

      function sanitizeLetters(value, max) {
        return value.replace(/[^A-Za-z\s]/g, '').slice(0, max);
      }
      function sanitizeEmail(value) {
        return value.replace(/\s/g, '').slice(0, 60);
      }
      function updateCounter() {
        if (!counter) return;
        const remaining = 160 - commentField.value.length;
        counter.textContent = `(${remaining} left)`;
      }
      if (nameField) nameField.addEventListener('input', () => nameField.value = sanitizeLetters(nameField.value, 40));
      if (emailField) emailField.addEventListener('input', () => emailField.value = sanitizeEmail(emailField.value));
      if (commentField) {
        commentField.addEventListener('input', () => {
          commentField.value = sanitizeLetters(commentField.value, 160);
          commentField.style.height = 'auto';
          commentField.style.height = Math.min(commentField.scrollHeight, 600) + 'px';
          updateCounter();
        });
        updateCounter();
      }
      if (form) {
        form.addEventListener('submit', function (e) {
          e.preventDefault();
          if (!statusEl) return;
          const rating = ratingInput.value;
          if (!rating || !nameField.value || !emailField.value || !commentField.value) {
            statusEl.textContent = 'Please complete all fields.';
            return;
          }
          statusEl.textContent = 'Submitting...';
          setTimeout(() => {
            statusEl.textContent = 'Thank you for your feedback!';
            form.reset();
            if (commentField) commentField.style.height = null;
            updateCounter();
            updateStars(0);
            ratingLabel.textContent = 'Select rating';
          }, 700);
        });
      }
    });
  </script>
</section>

<!-- Location Map Section -->
<section class="contact-map py-5">
  <div class="container" data-aos="fade-up">
    <div class="card border-0 shadow-sm p-4 neutral-card">
      <h4 class="fw-bold text-primary mb-4 text-center">Find Us</h4>
      <div class="map-container">
        <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d12345.67890!2d120.9679!3d15.3634!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3396f8b8b8b8b8b8%3A0x1234567890abcdef!2sSan%20Leonardo%2C%20Nueva%20Ecija!5e0!3m2!1sen!2sph!4v1234567890123!5m2!1sen!2sph" width="100%" height="420" style="border:0;border-radius:16px;filter:brightness(0.9) contrast(1.05);" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
      </div>
      <div class="text-center mt-3">
        <p class="text-muted mb-2"><i class="bi bi-geo-alt-fill text-primary me-2"></i><strong>San Leonardo, Nueva Ecija</strong></p>
        <p class="text-muted small mb-0">Drop by our location or schedule an on-site visit in the area.</p>
      </div>
    </div>
  </div>
</section>
